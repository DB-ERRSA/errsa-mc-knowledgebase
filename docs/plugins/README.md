# Plugins

This section documents each plugin: what it does, config locations, permissions, and common issues.

## Add a new plugin page
Create a file in `docs/plugins/<plugin>.md` and add it to `mkdocs.yml` under **Plugins**.

